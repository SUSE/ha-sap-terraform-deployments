{
  "variable": {
    "azure_region": {
      "type": "string",
      "default": "westeurope",
      "description": "Azure region where the machines will be created"
    },
    "ssh_authorized_keys": {
      "type": "list",
      "description": "List of additional authorized SSH public keys paths to access the created machines with the OS admin user"
    },
    "system_identifier": {
      "type": "string",
      "default": "prd",
      "description": "HANA SID. The System identifier (SID) must be 3 letters]"
    },
    "instance_number": {
      "type": "string",
      "default": "00",
      "description": "HANA system instance number. Instance number must be 2 digits]"
    },
    "sap_admin_password": {
      "type": "string",
      "description": "SAP HANA administrator password"
    },
    "deployment_type": {
      "type": "string",
      "default": "HA",
      "description": "Select between HA (SAP HANA system replication and HA capabilities) or non HA deployment. Options=[\"HA\",\"non HA\"]"
    },
    "instance_type": {
      "type": "string",
      "description": "Only used to create the 'Size cluster' page"
    },
    "hana_installation_software_path": {
      "type": "string",
      "description": "Azure storage account file share path, where the downloaded SAP HANA installation media is available"
    },
    "storage_account_name": {
      "type": "string",
      "description": "Azure storage account name of the file share"
    },
    "storage_account_key": {
      "type": "string",
      "description": "password_key: Azure storage account access key"
    }
  }
}
